{
  "name": "scrt-prediction-contracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "Apache-2.0",
  "scripts": {
    "clean": "cargo clean && rm -rf artifacts",
    "compile": "polar compile",
    "recompile": "yarn clean && yarn compile",
    "test": "polar test"
  },
  "dependencies": {
    "@types/chai": "^4.3.0",
    "chai": "^4.3.4",
    "secret-polar": "^0.9.5",
    "secretjs": "^0.17.5"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "prettier": "^2.5.1"
  }
}
